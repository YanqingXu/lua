# 下一步开发计划

**日期**: 2025-11-14  
**项目目标**: 完全兼容Lua 5.1.5  
**当前状态**: M6里程碑已完成（Task 3完成），项目整体进度52%

---

## 📋 执行摘要

基于架构分析报告的发现，本计划针对**完全兼容Lua 5.1.5**的目标，制定了详细的开发路线图。

**关键发现**：
- ✅ Task 3已完成：VM现在支持Lua函数调用（8/8测试通过）
- 🔴 严重缺陷：缺少环境表机制（影响setfenv/getfenv、沙箱、模块系统）
- 🟡 中等问题：VM.base_未使用、C函数upvalue存储方式不同
- 📊 当前兼容性：核心功能85%，API 70%，语义80%

**下一步重点**：
1. **P0（立即）**：添加环境表支持（2-4小时）
2. **P1（本周）**：优化VM.base_使用（1-2小时）
3. **P1（本周）**：完善CodeGenerator支持for循环（4-6小时）
4. **P2（下周）**：扩展标准库（表库、字符串库、数学库）（1-2周）

---

## 🎯 优先级清单

### P0：立即修复（本日完成）

#### ✅ Task P0-1：添加环境表支持

**问题描述**：Function类缺少env字段，导致无法实现Lua 5.1的环境表机制

**影响范围**：
- ❌ 无法实现setfenv/getfenv函数（Lua 5.1核心API）
- ❌ 无法实现沙箱机制（安全执行）
- ❌ 模块系统受影响（package.seeall等）
- ❌ 所有函数共享同一个全局表（语义不兼容）

**严重程度**：🔴 严重（阻碍Lua 5.1完全兼容）

**工作量估算**：2-4小时

**依赖关系**：无

**实施步骤**：

1. **修改Function类**（`src/core/function.hpp`）
2. **修改Function构造函数**（`src/core/function.cpp`）
3. **修改GETGLOBAL/SETGLOBAL指令**（`src/vm/vm.cpp`）
4. **修改mark()方法**（`src/core/function.cpp`）
5. **添加测试用例**（`tests/test_env.cpp`）

**详细技术方案**：见下文"技术方案详解"部分

**验收标准**：
- ✅ Function类有env_字段
- ✅ GETGLOBAL/SETGLOBAL使用函数的环境表
- ✅ 可以通过setEnv()设置函数的环境表
- ✅ GC正确标记环境表
- ✅ 测试通过：不同函数可以有不同的环境表

---

### P1：本周完成（1-2天）

#### ✅ Task P1-1：优化VM.base_字段使用

**问题描述**：VM.base_字段声明但未使用，每次R()调用都需要查找CallInfo

**影响范围**：
- ⚠️ 性能略低（每次R()调用都有额外开销）
- ⚠️ 代码混淆（字段存在但未使用）

**严重程度**：🟡 中等（功能正常但不够优化）

**工作量估算**：1-2小时

**依赖关系**：无

**实施步骤**：

**方案A（推荐）**：使用base_缓存
1. 修改executeProto()设置base_
2. 修改R()方法使用base_
3. 注意栈重新分配时更新base_
4. 性能测试验证提升

**方案B**：移除base_字段
1. 删除VM.base_字段
2. R()保持现有实现
3. 代码更清晰

**推荐**：方案A（与标准Lua一致，性能更好）

**验收标准**：
- ✅ base_字段被正确使用或移除
- ✅ 所有VM测试通过
- ✅ 性能测试显示改进（如果使用方案A）

---

#### ✅ Task P1-2：完善CodeGenerator支持for循环

**问题描述**：CodeGenerator不支持for循环（数值for和泛型for）

**影响范围**：
- ❌ 无法编译包含for循环的Lua脚本
- ❌ 限制了可运行的Lua代码范围

**严重程度**：🟡 中等（基本功能缺失）

**工作量估算**：4-6小时

**依赖关系**：无

**实施步骤**：

1. **分析标准Lua的for循环实现**（`lua_c_analysis/src/lcode.c`）
2. **实现数值for循环**：
   - FORPREP指令生成
   - FORLOOP指令生成
   - 循环变量管理
3. **实现泛型for循环**：
   - TFORLOOP指令生成
   - 迭代器调用
4. **添加测试用例**

**验收标准**：
- ✅ 可以编译数值for循环：`for i=1,10 do ... end`
- ✅ 可以编译泛型for循环：`for k,v in pairs(t) do ... end`
- ✅ 生成的字节码正确
- ✅ VM可以正确执行for循环

---

### P2：下周完成（1-2周）

#### ✅ Task P2-1：扩展标准库

**目标**：实现表库、字符串库、数学库的核心函数

**当前状态**：基础库8/21函数（38%）

**工作量估算**：1-2周

**优先级顺序**：

**1. 表库（table）**（2-3天）
- `table.insert(t, [pos,] value)`
- `table.remove(t, [pos])`
- `table.concat(t, [sep, [i, [j]]])`
- `table.sort(t, [comp])`
- `table.getn(t)` / `#t`

**2. 字符串库（string）**（3-4天）
- `string.len(s)`
- `string.sub(s, i, [j])`
- `string.find(s, pattern, [init, [plain]])`
- `string.format(formatstring, ...)`
- `string.gsub(s, pattern, repl, [n])`
- `string.byte(s, [i, [j]])`
- `string.char(...)`

**3. 数学库（math）**（1-2天）
- `math.abs(x)`
- `math.floor(x)`, `math.ceil(x)`
- `math.max(...)`, `math.min(...)`
- `math.sqrt(x)`, `math.pow(x, y)`
- `math.sin(x)`, `math.cos(x)`, `math.tan(x)`
- `math.random([m, [n]])`, `math.randomseed(x)`

**参考实现**：
- `lua_c_analysis/src/ltablib.c`
- `lua_c_analysis/src/lstrlib.c`
- `lua_c_analysis/src/lmathlib.c`

**验收标准**：
- ✅ 每个库至少实现80%的核心函数
- ✅ 每个函数有对应的测试用例
- ✅ 与Lua 5.1.5行为一致

---

#### ✅ Task P2-2：实现基础C API

**目标**：支持从C++代码调用Lua

**工作量估算**：3-5天

**必须实现的API**：

**栈操作**：
- `lua_pushstring`, `lua_pushnumber`, `lua_pushboolean`, `lua_pushnil`
- `lua_pop`, `lua_gettop`, `lua_settop`
- `lua_pushvalue`, `lua_remove`, `lua_insert`

**类型检查**：
- `lua_type`, `lua_typename`
- `lua_isstring`, `lua_isnumber`, `lua_isboolean`, `lua_isfunction`, `lua_istable`

**类型转换**：
- `lua_tostring`, `lua_tonumber`, `lua_toboolean`

**表操作**：
- `lua_newtable`, `lua_createtable`
- `lua_gettable`, `lua_settable`
- `lua_getfield`, `lua_setfield`
- `lua_rawget`, `lua_rawset`

**函数调用**：
- `lua_call`, `lua_pcall`

**全局变量**：
- `lua_getglobal`, `lua_setglobal`

**环境表**（依赖P0-1）：
- `lua_getfenv`, `lua_setfenv`

**参考实现**：`lua_c_analysis/src/lapi.c`

**验收标准**：
- ✅ 实现至少30个核心API函数
- ✅ 每个API有对应的测试用例
- ✅ 可以从C++代码调用Lua函数
- ✅ 可以从Lua代码调用C++函数

---

### P3：可延后（1-2个月）

#### ✅ Task P3-1：优化C函数upvalue存储方式

**问题描述**：C函数和Lua函数都使用Upvalue*存储upvalue，与标准Lua不同

**影响**：内存开销略高，性能略低，语义不同

**建议**：延后到性能优化阶段

---

#### ✅ Task P3-2：优化函数调用机制

**问题描述**：使用递归而非reentry实现函数调用

**影响**：性能略低，C++栈使用略高

**建议**：当前实现可接受，延后到性能优化阶段

---

#### ✅ Task P3-3：实现IO库、OS库

**工作量估算**：1-2周

**功能**：
- IO库：`io.open`, `io.close`, `io.read`, `io.write`, `io.lines`
- OS库：`os.clock`, `os.date`, `os.time`, `os.execute`, `os.getenv`

---

#### ✅ Task P3-4：实现协程系统

**工作量估算**：2-3周

**功能**：
- `coroutine.create`, `coroutine.resume`, `coroutine.yield`
- `coroutine.status`, `coroutine.running`

---

#### ✅ Task P3-5：实现调试库

**工作量估算**：1-2周

**功能**：
- `debug.traceback`, `debug.getinfo`, `debug.getlocal`
- `debug.setlocal`, `debug.getupvalue`, `debug.setupvalue`

---

## 🏁 里程碑规划

### M6.5：完全兼容的基础版本（本周完成）

**目标**：修复所有严重不兼容问题，达到Lua 5.1基础兼容

**任务列表**：
- ✅ Task 3：VM支持Lua函数调用（已完成）
- ⏳ Task P0-1：添加环境表支持（2-4小时）
- ⏳ Task P1-1：优化VM.base_使用（1-2小时）
- ⏳ Task P1-2：完善CodeGenerator支持for循环（4-6小时）

**预计完成时间**：2025-11-15（1天）

**验收标准**：
```lua
-- 测试1：环境表
function test_env()
    local env1 = {x = 10}
    local env2 = {x = 20}
    
    local f1 = function() return x end
    local f2 = function() return x end
    
    setfenv(f1, env1)
    setfenv(f2, env2)
    
    assert(f1() == 10)
    assert(f2() == 20)
end

-- 测试2：for循环
function test_for()
    local sum = 0
    for i = 1, 10 do
        sum = sum + i
    end
    assert(sum == 55)
    
    local t = {a=1, b=2, c=3}
    local count = 0
    for k, v in pairs(t) do
        count = count + 1
    end
    assert(count == 3)
end
```

**兼容性提升**：
- 核心功能：85% → 95%
- API兼容性：70% → 85%
- 语义兼容性：80% → 95%

---

### M7：实用版本（2周完成）

**目标**：实现常用标准库，可以运行中等复杂度的Lua脚本

**任务列表**：
- ✅ Task P2-1：扩展标准库（表库、字符串库、数学库）
- ✅ Task P2-2：实现基础C API

**预计完成时间**：2025-11-28（2周）

**验收标准**：
```lua
-- 可以运行此脚本
local t = {3, 1, 4, 1, 5, 9, 2, 6}
table.sort(t)
print(table.concat(t, ", "))

local s = "Hello, Lua!"
print(string.sub(s, 1, 5))
print(string.format("Pi = %.2f", math.pi))

for i = 1, 10 do
    print(math.sqrt(i))
end
```

**兼容性提升**：
- 标准库：38% → 70%
- C API：0% → 60%

---

### M8：完整版本（3个月完成）

**目标**：完全兼容Lua 5.1.5，通过官方测试套件

**任务列表**：
- ✅ Task P3-3：实现IO库、OS库
- ✅ Task P3-4：实现协程系统
- ✅ Task P3-5：实现调试库
- ✅ 完整的C API
- ✅ Lua 5.1.5官方测试套件集成
- ✅ 性能优化

**预计完成时间**：2026-02-14（3个月）

**验收标准**：
- ✅ 通过Lua 5.1.5官方测试套件
- ✅ 标准库100%兼容
- ✅ C API 100%兼容
- ✅ 性能达到标准Lua的80%以上

---

## 🔧 技术方案详解

### 方案1：添加环境表支持（P0-1）

#### 1.1 修改Function类

**文件**：`src/core/function.hpp`

```cpp
class Function : public GCObject {
public:
    explicit Function(CFunction func);
    explicit Function(Proto* proto);
    ~Function() override;
    
    // 类型检查
    bool isCFunction() const noexcept { return isC_; }
    bool isLuaFunction() const noexcept { return !isC_; }
    
    // 访问器
    CFunction getCFunction() const noexcept { return isC_ ? cFunction_ : nullptr; }
    Proto* getProto() const noexcept { return isC_ ? nullptr : proto_; }
    
    // Upvalue管理
    usize getUpvalueCount() const noexcept { return upvalues_.size(); }
    Upvalue* getUpvalue(usize index) const;
    void setUpvalue(usize index, Upvalue* upvalue);
    void addUpvalue(Upvalue* upvalue);
    
    // 环境表管理（新增）
    Table* getEnv() const noexcept { return env_; }
    void setEnv(Table* env) noexcept { env_ = env; }
    
    // GC接口
    void mark() override;
    usize getSize() const override;
    
private:
    bool isC_;                  // 是否为C函数
    CFunction cFunction_;       // C函数指针
    Proto* proto_;              // 函数原型
    Vec<Upvalue*> upvalues_;    // Upvalue数组
    Table* env_;                // 环境表（新增）
};
```

#### 1.2 修改Function构造函数

**文件**：`src/core/function.cpp`

```cpp
Function::Function(CFunction func)
    : GCObject(GCObjectType::FUNCTION)
    , isC_(true)
    , cFunction_(func)
    , proto_(nullptr)
    , env_(nullptr)  // 新增：初始化为nullptr
{
}

Function::Function(Proto* proto)
    : GCObject(GCObjectType::FUNCTION)
    , isC_(false)
    , cFunction_(nullptr)
    , proto_(proto)
    , env_(nullptr)  // 新增：初始化为nullptr
{
}
```

#### 1.3 修改mark()方法

**文件**：`src/core/function.cpp`

```cpp
void Function::mark() {
    if (isMarked()) {
        return;
    }

    setMarked(true);

    if (isC_) {
        // 标记C函数的upvalues
        for (Upvalue* uv : upvalues_) {
            if (uv) {
                uv->mark();
            }
        }
    } else {
        // 标记Lua函数的proto和upvalues
        if (proto_) {
            proto_->mark();
        }
        for (Upvalue* uv : upvalues_) {
            if (uv) {
                uv->mark();
            }
        }
    }

    // 标记环境表（新增）
    if (env_) {
        env_->mark();
    }
}
```

#### 1.4 修改GETGLOBAL指令

**文件**：`src/vm/vm.cpp`

```cpp
case OpCode::GETGLOBAL: {
    i32 bx = GETARG_Bx(inst);
    GCString* name = currentProto_->getConstant(bx).asString();

    // 使用当前函数的环境表（修改）
    Table* env = currentFunc_->getEnv();
    if (!env) {
        // 如果函数没有环境表，使用全局表
        env = L_->getGlobalTable();
    }

    Value value = env->get(Value(name));
    R(a) = value;
    break;
}
```

#### 1.5 修改SETGLOBAL指令

**文件**：`src/vm/vm.cpp`

```cpp
case OpCode::SETGLOBAL: {
    i32 bx = GETARG_Bx(inst);
    GCString* name = currentProto_->getConstant(bx).asString();

    // 使用当前函数的环境表（修改）
    Table* env = currentFunc_->getEnv();
    if (!env) {
        // 如果函数没有环境表，使用全局表
        env = L_->getGlobalTable();
    }

    env->set(Value(name), R(a));
    break;
}
```

#### 1.6 在函数创建时设置环境表

**文件**：`src/compiler/code_generator.cpp`（或VM中创建函数的地方）

```cpp
// 创建Lua函数时
Function* func = new Function(proto);
func->setEnv(L->getGlobalTable());  // 默认使用全局表
L->getGlobalState().getGC().registerObject(func);
```

#### 1.7 实现setfenv/getfenv基础库函数

**文件**：`src/lib/base_lib.cpp`

```cpp
// getfenv([f])
static i32 base_getfenv(LuaState* L) {
    Stack& stack = L->getStack();

    // 获取参数（函数或栈级别）
    Value arg = stack.size() > 0 ? stack.at(0) : Value(1.0);

    if (arg.isFunction()) {
        // 直接获取函数的环境表
        Function* func = arg.asFunction();
        Table* env = func->getEnv();
        if (env) {
            stack.push(Value(env));
        } else {
            stack.push(Value(L->getGlobalTable()));
        }
    } else if (arg.isNumber()) {
        // 根据栈级别获取函数
        i32 level = static_cast<i32>(arg.asNumber());
        if (level == 0) {
            // 返回当前线程的全局表
            stack.push(Value(L->getGlobalTable()));
        } else {
            // TODO: 实现栈级别查找
            stack.push(Value(L->getGlobalTable()));
        }
    } else {
        stack.push(Value());  // nil
    }

    return 1;
}

// setfenv(f, table)
static i32 base_setfenv(LuaState* L) {
    Stack& stack = L->getStack();

    if (stack.size() < 2) {
        throw std::runtime_error("setfenv: insufficient arguments");
    }

    Value arg = stack.at(0);
    Value envVal = stack.at(1);

    if (!envVal.isTable()) {
        throw std::runtime_error("setfenv: environment must be a table");
    }

    Table* env = envVal.asTable();

    if (arg.isFunction()) {
        Function* func = arg.asFunction();
        func->setEnv(env);
        stack.push(arg);  // 返回函数
        return 1;
    } else if (arg.isNumber()) {
        i32 level = static_cast<i32>(arg.asNumber());
        if (level == 0) {
            // 设置当前线程的全局表
            L->setGlobalTable(env);
            return 0;
        } else {
            // TODO: 实现栈级别查找
            throw std::runtime_error("setfenv: stack level not implemented");
        }
    } else {
        throw std::runtime_error("setfenv: invalid argument");
    }
}
```

#### 1.8 添加测试用例

**文件**：`tests/test_env.cpp`（新建）

```cpp
#include <gtest/gtest.h>
#include "vm/lua_state.hpp"
#include "core/function.hpp"
#include "core/table.hpp"

TEST(EnvTest, FunctionHasEnv) {
    GlobalState gs;
    LuaState L(gs);

    // 创建函数
    Proto* proto = new Proto();
    Function* func = new Function(proto);

    // 默认环境表为nullptr
    EXPECT_EQ(func->getEnv(), nullptr);

    // 设置环境表
    Table* env = new Table();
    func->setEnv(env);
    EXPECT_EQ(func->getEnv(), env);
}

TEST(EnvTest, DifferentFunctionsHaveDifferentEnvs) {
    GlobalState gs;
    LuaState L(gs);

    // 创建两个函数
    Proto* proto1 = new Proto();
    Proto* proto2 = new Proto();
    Function* func1 = new Function(proto1);
    Function* func2 = new Function(proto2);

    // 设置不同的环境表
    Table* env1 = new Table();
    Table* env2 = new Table();
    env1->set(Value("x"), Value(10.0));
    env2->set(Value("x"), Value(20.0));

    func1->setEnv(env1);
    func2->setEnv(env2);

    // 验证环境表不同
    EXPECT_NE(func1->getEnv(), func2->getEnv());
    EXPECT_EQ(func1->getEnv()->get(Value("x")).asNumber(), 10.0);
    EXPECT_EQ(func2->getEnv()->get(Value("x")).asNumber(), 20.0);
}

TEST(EnvTest, GCMarksEnv) {
    GlobalState gs;
    LuaState L(gs);

    Proto* proto = new Proto();
    Function* func = new Function(proto);
    Table* env = new Table();

    func->setEnv(env);

    // 标记函数应该同时标记环境表
    func->mark();
    EXPECT_TRUE(env->isMarked());
}
```

---

### 方案2：优化VM.base_使用（P1-1）

#### 2.1 方案A：使用base_缓存（推荐）

**文件**：`src/vm/vm.cpp`

```cpp
void VM::executeProto(Proto* proto, i32 nexeccalls) {
    if (nexeccalls > MAX_CALLS) {
        throw std::runtime_error("VM: stack overflow (too many nested calls)");
    }

    currentProto_ = proto;
    pc_ = 0;

    CallInfo& ci = L_->getCurrentCallInfo();
    Stack& stack = L_->getStack();

    // 缓存base指针（修改）
    base_ = &stack.at(ci.base);

    // 确保栈有足够的空间
    usize requiredTop = ci.base + proto->getMaxStackSize();
    while (stack.size() < requiredTop) {
        stack.push(Value());
    }

    // 注意：栈可能重新分配，需要更新base_
    base_ = &stack.at(ci.base);

    // 主执行循环
    const Vec<Instruction>& code = proto->getCode();
    while (pc_ < code.size()) {
        Instruction inst = code[pc_++];
        // ... 指令分发
    }
}

// 简化R()方法
Value& VM::R(i32 index) {
    return base_[index];  // 直接数组访问
}
```

**注意事项**：
- 栈重新分配时需要更新base_
- 在CALL指令中，调用子函数返回后需要恢复base_

#### 2.2 方案B：移除base_字段

**文件**：`src/vm/vm.hpp`

```cpp
class VM {
public:
    explicit VM(LuaState* L);
    void execute(Function* func);
    void executeProto(Proto* proto, i32 nexeccalls = 1);

private:
    LuaState* L_;
    Proto* currentProto_;
    usize pc_;
    // 移除：Value* base_;
    Function* currentFunc_;

    // ... 其他方法
};
```

**文件**：`src/vm/vm.cpp`

```cpp
// R()方法保持不变
Value& VM::R(i32 index) {
    CallInfo& ci = L_->getCurrentCallInfo();
    Stack& stack = L_->getStack();
    usize absIndex = ci.base + index;

    if (absIndex >= stack.size()) {
        throw std::runtime_error("VM::R: register index out of range");
    }

    return stack.at(absIndex);
}
```

---

### 方案3：完善CodeGenerator支持for循环（P1-2）

#### 3.1 数值for循环

**Lua代码**：
```lua
for i = 1, 10, 2 do
    print(i)
end
```

**字节码**：
```
LOADK    R(base+0) 1      ; 初始值
LOADK    R(base+1) 10     ; 限制值
LOADK    R(base+2) 2      ; 步长
FORPREP  R(base+0) label  ; 准备循环
label:
MOVE     R(base+3) R(base+0)  ; 循环变量
CALL     print
FORLOOP  R(base+0) label  ; 循环
```

**实现**（`src/compiler/code_generator.cpp`）：

```cpp
void CodeGenerator::visitForNumStatement(ForNumStatement* node) {
    // 生成初始值、限制值、步长
    node->init->accept(this);
    i32 initReg = lastReg_;

    node->limit->accept(this);
    i32 limitReg = lastReg_;

    if (node->step) {
        node->step->accept(this);
    } else {
        // 默认步长为1
        emitLoadK(allocateRegister(), Value(1.0));
    }
    i32 stepReg = lastReg_;

    // FORPREP指令
    usize forprepPC = emitInstruction(CREATE_ABC(OpCode::FORPREP, initReg, 0, 0));

    // 循环变量
    i32 varReg = allocateRegister();
    currentScope_->defineLocal(node->varName);

    // 循环体
    node->body->accept(this);

    // FORLOOP指令
    i32 jump = static_cast<i32>(forprepPC) - static_cast<i32>(currentInstructions().size());
    emitInstruction(CREATE_ABC(OpCode::FORLOOP, initReg, jump, 0));

    // 回填FORPREP的跳转目标
    i32 loopJump = static_cast<i32>(currentInstructions().size()) - static_cast<i32>(forprepPC) - 1;
    currentInstructions()[forprepPC] = CREATE_ABC(OpCode::FORPREP, initReg, loopJump, 0);
}
```

#### 3.2 泛型for循环

**Lua代码**：
```lua
for k, v in pairs(t) do
    print(k, v)
end
```

**字节码**：
```
GETTABUP R(base+0) _ENV "pairs"
GETTABUP R(base+1) _ENV "t"
CALL     R(base+0) 2 4    ; pairs(t) -> f, s, var
JMP      label
label_loop:
MOVE     R(base+4) R(base+0)  ; k
MOVE     R(base+5) R(base+1)  ; v
CALL     print
label:
TFORLOOP R(base+0) 2      ; 调用迭代器
JMP      label_loop
```

**实现**（`src/compiler/code_generator.cpp`）：

```cpp
void CodeGenerator::visitForInStatement(ForInStatement* node) {
    // 生成迭代器表达式
    for (auto* expr : node->exprs) {
        expr->accept(this);
    }

    i32 baseReg = lastReg_ - node->exprs.size() + 1;

    // 调用迭代器获取f, s, var
    emitCall(baseReg, node->exprs.size(), 3);

    // 跳转到TFORLOOP
    usize jmpPC = emitJump(0);

    // 循环体开始
    usize loopStart = currentInstructions().size();

    // 定义循环变量
    for (usize i = 0; i < node->varNames.size(); i++) {
        i32 varReg = allocateRegister();
        currentScope_->defineLocal(node->varNames[i]);
    }

    // 循环体
    node->body->accept(this);

    // 回填跳转
    patchJump(jmpPC, currentInstructions().size());

    // TFORLOOP指令
    emitInstruction(CREATE_ABC(OpCode::TFORLOOP, baseReg, 0, node->varNames.size()));

    // 跳回循环开始
    i32 jump = static_cast<i32>(loopStart) - static_cast<i32>(currentInstructions().size()) - 1;
    emitJump(jump);
}
```

---

## 📊 测试策略

### 1. 单元测试

**每个任务都需要对应的单元测试**：

- **P0-1（环境表）**：`tests/test_env.cpp`
  - 测试Function.getEnv()/setEnv()
  - 测试不同函数有不同环境表
  - 测试GC正确标记环境表
  - 测试GETGLOBAL/SETGLOBAL使用环境表

- **P1-1（VM.base_）**：`tests/test_vm.cpp`
  - 性能基准测试（对比优化前后）
  - 所有现有VM测试必须通过

- **P1-2（for循环）**：`tests/test_codegen_for.cpp`
  - 测试数值for循环生成
  - 测试泛型for循环生成
  - 测试嵌套for循环
  - 测试break语句

- **P2-1（标准库）**：`tests/test_table_lib.cpp`, `tests/test_string_lib.cpp`, `tests/test_math_lib.cpp`
  - 每个库函数至少2个测试用例
  - 边界条件测试
  - 错误处理测试

### 2. 集成测试

**创建Lua脚本测试套件**：

**文件**：`tests/lua_scripts/test_env.lua`
```lua
-- 测试环境表
function test_env()
    local env1 = {x = 10, print = print}
    local env2 = {x = 20, print = print}

    local f1 = function() print(x) end
    local f2 = function() print(x) end

    setfenv(f1, env1)
    setfenv(f2, env2)

    f1()  -- 应该输出10
    f2()  -- 应该输出20
end

test_env()
```

**文件**：`tests/lua_scripts/test_for.lua`
```lua
-- 测试for循环
function test_for_num()
    local sum = 0
    for i = 1, 10 do
        sum = sum + i
    end
    assert(sum == 55, "for loop failed")
end

function test_for_in()
    local t = {a=1, b=2, c=3}
    local count = 0
    for k, v in pairs(t) do
        count = count + 1
    end
    assert(count == 3, "for-in loop failed")
end

test_for_num()
test_for_in()
```

### 3. Lua 5.1.5官方测试套件集成

**步骤**：

1. **下载Lua 5.1.5测试套件**
   - 从Lua官网下载：https://www.lua.org/tests/5.1/
   - 或使用lua_c_analysis中的测试

2. **创建测试运行器**
   ```cpp
   // tests/lua51_test_runner.cpp
   int main(int argc, char** argv) {
       GlobalState gs;
       LuaState L(gs);

       // 加载并运行Lua测试脚本
       for (const auto& testFile : testFiles) {
           try {
               L.doFile(testFile);
               std::cout << "PASS: " << testFile << std::endl;
           } catch (const std::exception& e) {
               std::cerr << "FAIL: " << testFile << ": " << e.what() << std::endl;
           }
       }
   }
   ```

3. **逐步启用测试**
   - M6.5：基础测试（算术、逻辑、函数调用）
   - M7：标准库测试
   - M8：完整测试套件

### 4. 性能基准测试

**创建性能测试**：

```cpp
// tests/benchmark.cpp
#include <benchmark/benchmark.h>

static void BM_FunctionCall(benchmark::State& state) {
    GlobalState gs;
    LuaState L(gs);

    // 创建测试函数
    const char* code = R"(
        function fib(n)
            if n <= 1 then return n end
            return fib(n-1) + fib(n-2)
        end
    )";

    L.doString(code);

    for (auto _ : state) {
        L.call("fib", 20);
    }
}

BENCHMARK(BM_FunctionCall);
```

---

## ⚠️ 风险评估

### 风险1：环境表实现可能影响现有代码

**风险等级**：🟡 中等

**描述**：添加环境表后，GETGLOBAL/SETGLOBAL行为改变，可能影响现有测试

**缓解措施**：
1. 在函数创建时默认设置env为全局表，保持向后兼容
2. 运行所有现有测试，确保无回归
3. 逐步启用环境表功能

### 风险2：VM.base_优化可能引入栈重新分配bug

**风险等级**：🟡 中等

**描述**：使用base_缓存后，栈重新分配时base_可能失效

**缓解措施**：
1. 在所有可能导致栈重新分配的地方更新base_
2. 添加断言检查base_有效性
3. 充分测试嵌套函数调用和大量局部变量的情况

### 风险3：for循环实现复杂度高

**风险等级**：🟢 低

**描述**：for循环涉及多个指令和跳转，实现可能出错

**缓解措施**：
1. 参考lua_c_analysis的实现
2. 先实现数值for，再实现泛型for
3. 充分测试各种边界情况

### 风险4：标准库实现工作量大

**风险等级**：🟡 中等

**描述**：标准库函数众多，实现和测试工作量大

**缓解措施**：
1. 优先实现最常用的函数
2. 参考lua_c_analysis的实现
3. 每个函数实现后立即测试
4. 可以分批实现，逐步完善

---

## 📅 时间线

| 里程碑 | 任务 | 预计时间 | 完成日期 |
|--------|------|----------|----------|
| **M6.5** | P0-1: 环境表支持 | 2-4小时 | 2025-11-14 |
| | P1-1: VM.base_优化 | 1-2小时 | 2025-11-14 |
| | P1-2: for循环支持 | 4-6小时 | 2025-11-15 |
| | **M6.5完成** | **1天** | **2025-11-15** |
| **M7** | P2-1: 表库 | 2-3天 | 2025-11-18 |
| | P2-1: 字符串库 | 3-4天 | 2025-11-22 |
| | P2-1: 数学库 | 1-2天 | 2025-11-24 |
| | P2-2: 基础C API | 3-5天 | 2025-11-28 |
| | **M7完成** | **2周** | **2025-11-28** |
| **M8** | P3-3: IO/OS库 | 1-2周 | 2025-12-12 |
| | P3-4: 协程系统 | 2-3周 | 2026-01-02 |
| | P3-5: 调试库 | 1-2周 | 2026-01-16 |
| | 完整C API | 1-2周 | 2026-01-30 |
| | 性能优化 | 1-2周 | 2026-02-14 |
| | **M8完成** | **3个月** | **2026-02-14** |

---

## 📝 总结

### 立即行动（今天）

1. ✅ **实现P0-1：添加环境表支持**（2-4小时）
   - 修改Function类
   - 修改GETGLOBAL/SETGLOBAL
   - 添加测试

2. ✅ **实现P1-1：优化VM.base_**（1-2小时）
   - 选择方案A或B
   - 修改代码
   - 运行测试

### 本周完成

3. ✅ **实现P1-2：for循环支持**（4-6小时）
   - 实现数值for
   - 实现泛型for
   - 添加测试

### 两周内完成

4. ✅ **实现P2-1：扩展标准库**（1-2周）
   - 表库
   - 字符串库
   - 数学库

5. ✅ **实现P2-2：基础C API**（3-5天）

### 长期目标

6. ✅ **M8：完整版本**（3个月）
   - IO/OS库
   - 协程系统
   - 调试库
   - 完整C API
   - 性能优化

---

**下一步**：开始实现P0-1（添加环境表支持）


