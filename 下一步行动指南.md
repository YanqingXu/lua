# 下一步行动指南

**日期**: 2025-11-14  
**项目目标**: 完全兼容Lua 5.1.5  
**当前状态**: M6完成，Task 3完成，项目进度52%

---

## 🎯 立即行动（今天完成）

### ✅ P0-1：添加环境表支持（2-4小时）

**为什么重要**：这是Lua 5.1.5的核心特性，缺少它会导致：
- ❌ 无法实现setfenv/getfenv
- ❌ 无法实现沙箱机制
- ❌ 模块系统受影响
- ❌ 与Lua 5.1.5语义不兼容

**要做什么**：

1. **修改`src/core/function.hpp`**：添加`Table* env_`字段
2. **修改`src/core/function.cpp`**：
   - 构造函数初始化`env_ = nullptr`
   - `mark()`方法标记`env_`
3. **修改`src/vm/vm.cpp`**：
   - GETGLOBAL使用`currentFunc_->getEnv()`
   - SETGLOBAL使用`currentFunc_->getEnv()`
4. **添加`tests/test_env.cpp`**：测试环境表功能

**验收标准**：
```lua
local env1 = {x = 10}
local env2 = {x = 20}
local f1 = function() return x end
local f2 = function() return x end
setfenv(f1, env1)
setfenv(f2, env2)
assert(f1() == 10)
assert(f2() == 20)
```

**详细代码**：见`下一步开发计划.md`第3.1节

---

### ✅ P1-1：优化VM.base_使用（1-2小时）

**为什么重要**：
- 当前base_字段未使用，造成混淆
- 每次R()调用都需要查找CallInfo，性能略低

**要做什么**：

**推荐方案A**：使用base_缓存
```cpp
// vm.cpp
void VM::executeProto(Proto* proto, i32 nexeccalls) {
    CallInfo& ci = L_->getCurrentCallInfo();
    Stack& stack = L_->getStack();
    base_ = &stack.at(ci.base);  // 缓存base指针
    // ...
}

Value& VM::R(i32 index) {
    return base_[index];  // 直接数组访问
}
```

**或方案B**：移除base_字段

**验收标准**：
- ✅ 所有VM测试通过
- ✅ 性能测试显示改进（方案A）

---

## 📅 本周完成（1-2天）

### ✅ P1-2：完善CodeGenerator支持for循环（4-6小时）

**要做什么**：

1. **实现数值for循环**：
   ```lua
   for i = 1, 10, 2 do
       print(i)
   end
   ```
   - 生成FORPREP指令
   - 生成FORLOOP指令

2. **实现泛型for循环**：
   ```lua
   for k, v in pairs(t) do
       print(k, v)
   end
   ```
   - 生成TFORLOOP指令
   - 处理迭代器调用

**验收标准**：
```lua
-- 数值for
local sum = 0
for i = 1, 10 do
    sum = sum + i
end
assert(sum == 55)

-- 泛型for
local t = {a=1, b=2, c=3}
local count = 0
for k, v in pairs(t) do
    count = count + 1
end
assert(count == 3)
```

**详细代码**：见`下一步开发计划.md`第3.3节

---

## 📊 两周内完成（M7里程碑）

### ✅ P2-1：扩展标准库（1-2周）

**优先级顺序**：

1. **表库**（2-3天）：`insert`, `remove`, `concat`, `sort`
2. **字符串库**（3-4天）：`len`, `sub`, `find`, `format`, `gsub`
3. **数学库**（1-2天）：`abs`, `floor`, `ceil`, `max`, `min`, `sqrt`, `sin`, `cos`

**参考**：`lua_c_analysis/src/ltablib.c`, `lstrlib.c`, `lmathlib.c`

---

### ✅ P2-2：实现基础C API（3-5天）

**必须实现**：
- 栈操作：`lua_pushstring`, `lua_pushnumber`, `lua_pop`, `lua_gettop`
- 类型检查：`lua_type`, `lua_isstring`, `lua_isnumber`
- 类型转换：`lua_tostring`, `lua_tonumber`
- 函数调用：`lua_call`, `lua_pcall`
- 全局变量：`lua_getglobal`, `lua_setglobal`
- 环境表：`lua_getfenv`, `lua_setfenv`

**参考**：`lua_c_analysis/src/lapi.c`

---

## 🏁 里程碑时间线

| 里程碑 | 目标 | 完成日期 |
|--------|------|----------|
| **M6** | VM支持Lua函数调用 | ✅ 2025-11-14 |
| **M6.5** | 修复严重不兼容问题 | 2025-11-15 |
| **M7** | 实用版本（标准库+C API） | 2025-11-28 |
| **M8** | 完整版本（100%兼容） | 2026-02-14 |

---

## 📈 兼容性提升预期

| 阶段 | 核心功能 | API | 语义 | 标准库 |
|------|----------|-----|------|--------|
| **当前（M6）** | 85% | 70% | 80% | 38% |
| **M6.5** | 95% | 85% | 95% | 38% |
| **M7** | 95% | 85% | 95% | 70% |
| **M8** | 100% | 100% | 100% | 100% |

---

## ⚠️ 关键风险

1. **环境表实现可能影响现有代码**
   - 缓解：默认使用全局表，保持向后兼容
   
2. **VM.base_优化可能引入栈重新分配bug**
   - 缓解：在栈重新分配后更新base_，充分测试

3. **标准库实现工作量大**
   - 缓解：优先实现常用函数，分批完成

---

## 📚 参考文档

- **详细开发计划**：`下一步开发计划.md`（1171行，包含所有技术方案）
- **架构分析报告**：`架构设计对比分析报告.md`（1011行）
- **架构分析总结**：`架构分析总结.md`（150行）

---

## ✅ 今天的任务清单

- [ ] 实现P0-1：添加环境表支持（2-4小时）
  - [ ] 修改Function类
  - [ ] 修改GETGLOBAL/SETGLOBAL
  - [ ] 添加测试
  - [ ] 运行所有测试确保无回归

- [ ] 实现P1-1：优化VM.base_（1-2小时）
  - [ ] 选择方案（推荐方案A）
  - [ ] 修改代码
  - [ ] 运行测试

- [ ] （可选）开始P1-2：for循环支持
  - [ ] 实现数值for
  - [ ] 添加测试

**预计今天完成**：P0-1 + P1-1，如果时间充裕可以开始P1-2

---

**下一步**：打开`src/core/function.hpp`，开始实现P0-1

