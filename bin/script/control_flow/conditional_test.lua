print("=== Conditional Expression Test ===")

print("Testing conditional expressions:")
local val = 10
print("  val =", val)

-- Test simple comparison (this might not work due to comparison issues)
-- local msg = val > 5 and "big" or "small"
-- print("  val > 5 ? 'big' : 'small' =", msg)

-- Test logical operations instead
local a = true
local b = false
print("  true and 'yes' =", a and "yes")
print("  false or 'no' =", b or "no")

print("Test complete")
