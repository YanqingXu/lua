{
  "schemaVersion": 1,
  "channel": "official-testc",
  "xfails": [
    {
      "id": "code.lua-opcode-sequence",
      "expectedDiagnostic": "code.lua LOADNIL fixture/oracle mismatch: expected RETURN; actual 1 - LOADNIL 1",
      "trackingIssue": "https://github.com/YanqingXu/lua/issues/4",
      "trackingStatus": "open in v0.1-embedding-preview",
      "responsibleModule": "compatibility fixture versioning and T.listcode bytecode oracle",
      "minimalReproduction": "bin/lua_test.exe --filter \"code.lua with T module XFAIL\"",
      "reason": "The first failing opcode check is the locked sequence-of-LOADNILs fixture: code.lua expects RETURN only, while this project and a Lua 5.1.5 luac comparison emit LOADNIL/LOADNIL/RETURN. The test traps that exact eighth opcode comparison with a unique diagnostic, so an unrelated assertion cannot satisfy this XFAIL."
    }
  ]
}
