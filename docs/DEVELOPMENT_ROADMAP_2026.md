# lua_in_cpp 项目开发路线图 2026

> **制定日期**: 2026-01-18  
> **当前完成度**: 78%  
> **剩余工作量**: 约17人日（22%）  
> **目标版本**: v1.0.0

---

## 📊 执行摘要

### 当前状态（更新于2026-01-18）

**已完成**（80%）⬆️ +2%：
- ✅ **编译器前端**：100%（词法、语法、代码生成）
- ✅ **虚拟机核心**：95%（38条指令全部实现）
- ✅ **类型系统**：100%（Value、GCObject、基础类型）
- ✅ **垃圾回收**：95%（标记-清除算法 + FIXED标志机制）⬆️ +5%
- ✅ **GlobalState初始化**：100%（元方法、保留字、固定字符串）🆕
- ✅ **数学库**：100%（22个函数）
- ✅ **I/O库**：44%（8/18函数）

**待完成**（20%）⬇️ -2%：
- 🔄 **基础库**：33%（8/24函数）
- ⏳ **字符串库**：0%（0/14函数）
- ⏳ **表库**：0%（0/7函数）
- ⏳ **OS库**：0%（0/11函数）
- ⏳ **TODO修复**：21处待修复
- ⏳ **集成测试**：待完善

### 核心目标

1. **M6里程碑**（标准库完成）：2周后（预计2026-02-01）
2. **M7里程碑**（1.0发布）：4-6周后（预计2026-02-15至2026-03-01）

### 最新进展（2026-01-18）

- ✅ **P0任务1完成**：LuaState初始化系统（2人日）
- ✅ **GC系统增强**：修复2个关键bug，实现FIXED标志机制
- ✅ **测试覆盖提升**：新增20个单元测试（总计399个）
- ✅ **整体完成度**：78% → 80%

---

## 🎯 优先级任务清单

### P0 - 关键任务（本周必须完成）⭐⭐⭐

#### ✅ 任务1：补充LuaState初始化步骤 [已完成 - 2026-01-18]

**完成状态**：✅ 100%完成（实际工作量：2人日）

**完成内容**：
- ✅ **字符串表初始化**
  - 在`StringPool`添加`resize()`方法
  - 在`GlobalState`构造函数中调用`stringPool.resize(32)`

- ✅ **元方法名称初始化**
  - 在`GlobalState`添加`tmname_`数组（17个元方法）
  - 初始化并标记为固定字符串
  - 实现`getMetamethodName(TMS event)`方法

- ✅ **保留字初始化**
  - 实现`initReservedWords()`方法（21个保留字）
  - 初始化并标记为固定字符串

- ✅ **内存错误消息固定**
  - 固定"not enough memory"字符串
  - 确保GC不会回收此字符串

- ⏸️ **GC阈值设置**（延后）
  - 原因：当前GC无自动触发机制
  - 计划：在实现增量GC时一并实现

**技术成就**：
- 实现了Lua 5.1.5兼容的FIXED标志机制
- 修复了GC系统的两个关键bug：
  1. `mark()`方法清除FIXED标志
  2. `clearAll()`方法删除固定对象
- 新增20个单元测试，全部通过

**验收结果**：
- ✅ 所有初始化步骤按Lua 5.1.5标准顺序执行
- ✅ 元方法和保留字正确初始化并标记为固定
- ✅ 通过所有20个新增单元测试
- ✅ 固定对象在GC过程中得到正确保护

**详细报告**：见 `docs/P0_TASK1_COMPLETION_REPORT.md`

**修改文件**：
- `src/core/string_pool.hpp/cpp`
- `src/core/gc_object.hpp`
- `src/core/gc_string.hpp`
- `src/vm/global_state.hpp/cpp`
- `src/gc/garbage_collector.cpp`
- `tests/unit/vm/test_lua_state_init.cpp`（新建）

---

#### 任务2：实现脚本执行功能 [1人日]

**问题描述**：
当前缺少完整的脚本执行流程，无法从文件加载和执行Lua脚本。

**任务清单**：
- [ ] **完善executeScript()函数**（0.5人日）
  - 实现文件读取逻辑
  - 集成完整编译执行流程（Lexer→Parser→CodeGen→VM）
  - 添加错误处理和报告
  
- [ ] **支持命令行参数**（0.25人日）
  - 实现`arg`全局表
  - 传递命令行参数到Lua脚本
  
- [ ] **添加REPL历史记录**（0.25人日）
  - 实现命令历史记录
  - 支持上下箭头导航历史

**验收标准**：
- 可以从文件加载和执行Lua脚本
- 错误信息清晰准确
- REPL支持历史记录
- 通过端到端测试

**文件修改**：
- `src/main.cpp`
- `src/repl.cpp`
- `src/vm/lua_state.cpp`

---

#### 任务3：修复高优先级TODO [1人日]

**问题描述**：
8处高优先级TODO影响核心功能，需要立即修复。

**任务清单**：
- [ ] **VM相关**（0.5人日）
  - 修复`vm.cpp`的CodeGenerator hack（临时解决方案）
  - 完善Upvalues处理（2处）
  - 文件：`src/vm/vm.cpp`
  
- [ ] **Table相关**（0.3人日）
  - 实现nil键错误抛出
  - 实现Userdata标记
  - 实现Thread标记
  - 文件：`src/core/table.cpp`
  
- [ ] **LuaState相关**（0.2人日）
  - 实现字符串转数字（`toNumber`）
  - 完善元表支持
  - 文件：`src/vm/lua_state.cpp`

**验收标准**：
- 所有高优先级TODO修复完成
- 通过相关单元测试
- 代码质量提升

**文件修改**：
- `src/vm/vm.cpp`
- `src/core/table.cpp`
- `src/vm/lua_state.cpp`

---

### P1 - 重要任务（下周目标）⭐⭐

#### 任务4：扩展基础库 [3人日]

**当前状态**：8/24函数（33%完成）

**已实现函数**（8个）：
- ✅ print
- ✅ type
- ✅ tostring
- ✅ tonumber
- ✅ error
- ✅ assert
- ✅ setmetatable
- ✅ getmetatable

**待实现函数**（16个）：

**高优先级**（1.5人日）：
- [ ] **pcall**(f, ...)（0.5人日）
  - 保护模式调用函数
  - 捕获错误并返回状态
  - 参考：`lua_c_analysis/src/lbaselib.c:luaB_pcall`
  
- [ ] **pairs**(t)（0.3人日）
  - 返回迭代器函数
  - 遍历表的所有键值对
  - 参考：`lua_c_analysis/src/lbaselib.c:luaB_pairs`
  
- [ ] **ipairs**(t)（0.3人日）
  - 返回迭代器函数
  - 遍历表的数组部分
  - 参考：`lua_c_analysis/src/lbaselib.c:luaB_ipairs`
  
- [ ] **next**(t, k)（0.2人日）
  - 返回表的下一个键值对
  - 参考：`lua_c_analysis/src/lbaselib.c:luaB_next`
  
- [ ] **select**(index, ...)（0.2人日）
  - 返回参数列表的子集
  - 参考：`lua_c_analysis/src/lbaselib.c:luaB_select`

**中优先级**（1人日）：
- [ ] **rawget**(t, k)（0.15人日）
- [ ] **rawset**(t, k, v)（0.15人日）
- [ ] **rawequal**(v1, v2)（0.1人日）
- [ ] **getfenv**(f)（0.2人日）
- [ ] **setfenv**(f, table)（0.2人日）
- [ ] **loadstring**(s, [chunkname])（0.2人日）

**低优先级**（0.5人日）：
- [ ] **loadfile**(filename)（0.15人日）
- [ ] **dofile**(filename)（0.1人日）
- [ ] **collectgarbage**(opt, arg)（0.15人日）
- [ ] **unpack**(list, [i], [j])（0.1人日）
- [ ] **xpcall**(f, err)（0.1人日）

**验收标准**：
- 所有函数符合Lua 5.1.5规范
- 通过官方测试用例
- 性能达标

**文件修改**：
- `src/lib/baselib.cpp`
- `src/lib/baselib.hpp`

---

#### 任务5：实现字符串库 [2人日]

**当前状态**：0/14函数（0%完成）

**待实现函数**（14个）：

**核心函数**（1人日）：
- [ ] **string.sub**(s, i, [j])（0.15人日）
  - 提取子字符串
  - 支持负索引
  
- [ ] **string.find**(s, pattern, [init], [plain])（0.2人日）
  - 查找模式匹配
  - 返回起始和结束位置
  
- [ ] **string.gsub**(s, pattern, repl, [n])（0.25人日）
  - 全局替换
  - 支持函数和表替换
  
- [ ] **string.match**(s, pattern, [init])（0.2人日）
  - 模式匹配
  - 返回捕获内容
  
- [ ] **string.gmatch**(s, pattern)（0.2人日）
  - 返回迭代器
  - 遍历所有匹配

**辅助函数**（1人日）：
- [ ] **string.len**(s)（0.1人日）
- [ ] **string.upper**(s)（0.1人日）
- [ ] **string.lower**(s)（0.1人日）
- [ ] **string.reverse**(s)（0.1人日）
- [ ] **string.rep**(s, n)（0.1人日）
- [ ] **string.format**(formatstring, ...)（0.3人日）
- [ ] **string.char**(...)（0.1人日）
- [ ] **string.byte**(s, [i], [j])（0.1人日）
- [ ] **string.dump**(function)（0.1人日）

**验收标准**：
- 支持Lua模式匹配语法
- 通过官方测试用例
- 性能优化

**文件创建**：
- `src/lib/stringlib.cpp`
- `src/lib/stringlib.hpp`

---

#### 任务6：实现表库 [1.5人日]

**当前状态**：0/7函数（0%完成）

**待实现函数**（7个）：

- [ ] **table.insert**(table, [pos], value)（0.25人日）
  - 插入元素到表
  - 支持指定位置插入
  
- [ ] **table.remove**(table, [pos])（0.2人日）
  - 移除并返回元素
  - 支持指定位置移除
  
- [ ] **table.sort**(table, [comp])（0.4人日）
  - 原地排序
  - 支持自定义比较函数
  - 使用快速排序算法
  
- [ ] **table.concat**(table, [sep], [i], [j])（0.25人日）
  - 连接表元素为字符串
  - 支持分隔符和范围
  
- [ ] **table.getn**(table)（0.1人日）
  - 返回表的长度
  
- [ ] **table.maxn**(table)（0.15人日）
  - 返回最大数字键
  
- [ ] **table.foreach**(table, f)（0.15人日）
  - 遍历表并调用函数（已废弃，但需兼容）

**验收标准**：
- 符合Lua 5.1.5规范
- 排序算法稳定高效
- 通过官方测试用例

**文件创建**：
- `src/lib/tablelib.cpp`
- `src/lib/tablelib.hpp`

---

### P2 - 次要任务（后续优化）⭐

#### 任务7：完善I/O库 [2人日]

**当前状态**：8/18函数（44%完成）

**已实现函数**（8个）：
- ✅ io.open
- ✅ io.close
- ✅ io.read
- ✅ io.write
- ✅ io.flush
- ✅ io.input
- ✅ io.output
- ✅ io.type

**待实现函数**（10个）：

- [ ] **io.lines**([filename])（0.3人日）
  - 返回迭代器
  - 逐行读取文件
  
- [ ] **io.tmpfile**()（0.2人日）
  - 创建临时文件
  
- [ ] **file:lines**()（0.2人日）
  - 文件行迭代器
  
- [ ] **file:setvbuf**(mode, [size])（0.2人日）
  - 设置缓冲模式
  
- [ ] **file:seek**([whence], [offset])（0.3人日）
  - 文件定位
  
- [ ] **file:read**(...)（0.3人日）
  - 完善读取模式
  
- [ ] **file:write**(...)（0.2人日）
  - 完善写入功能
  
- [ ] **io.popen**(prog, [mode])（0.3人日）
  - 执行命令并返回文件句柄（可选）

**验收标准**：
- 完整的文件I/O功能
- 迭代器正确实现
- 错误处理完善

**文件修改**：
- `src/lib/iolib.cpp`
- `src/lib/iolib.hpp`

---

#### 任务8：实现OS库 [1.5人日]

**当前状态**：0/11函数（0%完成）

**待实现函数**（11个）：

**时间日期函数**（0.6人日）：
- [ ] **os.time**([table])（0.2人日）
  - 返回当前时间或指定时间的时间戳
  
- [ ] **os.date**([format], [time])（0.25人日）
  - 格式化时间
  - 支持多种格式字符串
  
- [ ] **os.clock**()（0.1人日）
  - 返回CPU时间
  
- [ ] **os.difftime**(t2, t1)（0.05人日）
  - 计算时间差

**系统函数**（0.6人日）：
- [ ] **os.execute**([command])（0.2人日）
  - 执行系统命令
  
- [ ] **os.exit**([code])（0.1人日）
  - 退出程序
  
- [ ] **os.getenv**(varname)（0.15人日）
  - 获取环境变量
  
- [ ] **os.setlocale**(locale, [category])（0.15人日）
  - 设置本地化

**文件操作函数**（0.3人日）：
- [ ] **os.remove**(filename)（0.1人日）
  - 删除文件
  
- [ ] **os.rename**(oldname, newname)（0.1人日）
  - 重命名文件
  
- [ ] **os.tmpname**()（0.1人日）
  - 生成临时文件名

**验收标准**：
- 跨平台兼容性
- 错误处理完善
- 通过官方测试用例

**文件创建**：
- `src/lib/oslib.cpp`
- `src/lib/oslib.hpp`

---

#### 任务9：修复所有TODO标记 [2人日]

**总计**：21处TODO标记

**分类修复**：

**高优先级**（已在P0任务中）：
- ✅ VM相关（3处）
- ✅ LuaState相关（2处）
- ✅ Table相关（3处）

**中优先级**（1人日）：
- [ ] **I/O库**（2处，0.3人日）
  - 迭代器闭包实现（`io.lines`）
  - 完整I/O实现
  - 文件：`src/lib/iolib.cpp`
  
- [ ] **基础库**（3处，0.4人日）
  - 元方法调用（`tostring`的`__tostring`）
  - 位置信息（`error`的level参数）
  - 完整错误处理
  - 文件：`src/lib/baselib.cpp`
  
- [ ] **元表**（2处，0.3人日）
  - 全局类型元表初始化
  - Lua函数元方法支持
  - 文件：`src/core/metatable.cpp`

**低优先级**（1人日）：
- [ ] **CodeGen**（3处，0.6人日）
  - 添加实际行号（3处）
  - 文件：`src/compiler/codegen.cpp`
  
- [ ] **Function**（1处，0.2人日）
  - Userdata/Thread标记实现
  - 文件：`src/core/function.cpp`
  
- [ ] **优化改进**（2处，0.2人日）
  - 常量折叠优化
  - 死代码消除

**验收标准**：
- 所有TODO标记修复或转为Issue
- 代码质量提升
- 通过相关测试

---

#### 任务10：集成测试和性能优化 [2人日]

**任务清单**：

**集成测试**（1人日）：
- [ ] **创建端到端测试套件**（0.5人日）
  - 10-20个完整Lua脚本测试
  - 覆盖所有标准库函数
  - 测试错误处理
  
- [ ] **Lua官方测试套件**（0.5人日）
  - 运行部分官方测试用例
  - 修复兼容性问题
  - 记录通过率

**性能优化**（1人日）：
- [ ] **性能基准测试**（0.3人日）
  - 创建性能测试套件
  - 对比Lua 5.1.5性能
  - 识别性能瓶颈
  
- [ ] **热点分析和优化**（0.4人日）
  - 使用profiler分析热点
  - 优化关键路径
  - 减少内存分配
  
- [ ] **内存使用优化**（0.3人日）
  - 减少内存占用
  - 优化GC性能
  - 内存泄漏检测

**验收标准**：
- 集成测试通过率>90%
- 性能达到Lua 5.1.5的70%以上
- 无内存泄漏

**文件创建**：
- `tests/integration/`
- `tests/performance/`

---

## 📅 时间估算和里程碑

### 工作量汇总

| 优先级 | 任务数 | 总工作量 | 占比 |
|--------|--------|----------|------|
| **P0** | 3个 | 4人日 | 24% |
| **P1** | 3个 | 6.5人日 | 38% |
| **P2** | 4个 | 6.5人日 | 38% |
| **总计** | 10个 | **17人日** | 100% |

### 里程碑规划

#### M6：标准库完成（2周后）

**目标**：
- ✅ 基础库100%完成（24/24函数）
- ✅ 字符串库100%完成（14/14函数）
- ✅ 表库100%完成（7/7函数）
- ✅ I/O库100%完成（18/18函数）
- ✅ OS库100%完成（11/11函数）
- ✅ 数学库100%完成（20/20函数，已完成）

**工作量**：10.5人日（P0 + P1）

**时间规划**：
- 全职开发：2周
- 兼职开发（50%）：4周

---

#### M7：1.0发布（4-6周后）

**目标**：
- ✅ 所有TODO修复完成
- ✅ 集成测试通过率>90%
- ✅ 性能达标（Lua 5.1.5的70%以上）
- ✅ 文档完善
- ✅ 发布v1.0.0

**工作量**：17人日（P0 + P1 + P2）

**时间规划**：
- 全职开发：3-4周
- 兼职开发（50%）：6-8周

---

## 🔄 实施建议

### 开发顺序和依赖关系

**第1周**（P0任务）：
```
Day 1-2: 任务1 - LuaState初始化
Day 3: 任务2 - 脚本执行功能
Day 4: 任务3 - 修复高优先级TODO
```

**第2周**（P1任务 - 基础库）：
```
Day 5-7: 任务4 - 扩展基础库（16个函数）
```

**第3周**（P1任务 - 字符串库和表库）：
```
Day 8-9: 任务5 - 实现字符串库（14个函数）
Day 10-11: 任务6 - 实现表库（7个函数）
```

**第4周**（P2任务）：
```
Day 12-13: 任务7 - 完善I/O库（10个函数）
Day 14: 任务8 - 实现OS库（11个函数）
Day 15-16: 任务9 - 修复所有TODO
Day 17: 任务10 - 集成测试和性能优化
```

### 依赖关系图

```
任务1（LuaState初始化）
  ↓
任务2（脚本执行）
  ↓
任务3（修复TODO）
  ↓
任务4（基础库）→ 任务5（字符串库）→ 任务6（表库）
  ↓                ↓                    ↓
任务7（I/O库）→ 任务8（OS库）
  ↓                ↓
任务9（修复所有TODO）
  ↓
任务10（集成测试）
```

---

## ⚠️ 风险评估和缓解措施

### 风险1：字符串库模式匹配复杂度高

**风险等级**：🔴 高

**描述**：
Lua的模式匹配语法复杂，实现难度大，可能超出预期时间。

**缓解措施**：
1. 优先实现简单函数（len、upper、lower等）
2. 模式匹配函数可以先实现简化版本
3. 参考lua_c_analysis中的详细实现
4. 预留额外0.5人日缓冲时间

---

### 风险2：I/O库迭代器实现困难

**风险等级**：🟡 中

**描述**：
`io.lines`和`file:lines`需要实现闭包迭代器，涉及upvalue管理。

**缓解措施**：
1. 先实现基础I/O功能
2. 迭代器可以延后到P2阶段
3. 参考已实现的`pairs`/`ipairs`迭代器
4. 预留额外0.3人日缓冲时间

---

### 风险3：集成测试发现未知问题

**风险等级**：🟡 中

**描述**：
集成测试可能发现核心功能的隐藏bug，需要额外时间修复。

**缓解措施**：
1. 在P0阶段就开始编写简单的集成测试
2. 每完成一个库就进行测试
3. 预留2人日的缓冲时间用于bug修复
4. 优先修复影响功能的bug

---

### 风险4：性能不达标

**风险等级**：🟢 低

**描述**：
性能可能无法达到Lua 5.1.5的70%。

**缓解措施**：
1. 性能优化是P2任务，不影响功能完整性
2. 可以在1.0发布后继续优化
3. 先确保功能正确，再优化性能
4. 记录性能瓶颈，制定优化计划

---

## ✅ 质量保证措施

### 测试策略

**单元测试**：
- 每个新函数必须有对应的单元测试
- 测试覆盖率目标：>90%
- 使用Google Test框架

**集成测试**：
- 每周至少运行一次完整集成测试
- 测试覆盖所有标准库函数
- 测试错误处理和边界情况

**性能测试**：
- 每个里程碑进行性能基准测试
- 对比Lua 5.1.5性能
- 记录性能回归

### 代码审查

**审查清单**：
- [ ] 代码符合C++17标准
- [ ] 符合项目编码规范
- [ ] 有完整的文档注释
- [ ] 通过所有单元测试
- [ ] 无内存泄漏
- [ ] 无编译警告

### 文档要求

**必须文档**：
- 每个公共API必须有文档注释
- 复杂算法必须有实现说明
- 每个库必须有使用示例
- 更新CHANGELOG.md

---

## 📈 进度跟踪

### 每日进度报告

**格式**：
```markdown
## 2026-01-XX 进度报告

### 完成任务
- [x] 任务X.Y - 具体内容

### 遇到问题
- 问题描述
- 解决方案

### 明日计划
- [ ] 任务X.Y - 具体内容
```

### 每周总结

**格式**：
```markdown
## 第X周总结（2026-01-XX ~ 2026-01-XX）

### 完成情况
- 计划任务：X个
- 完成任务：Y个
- 完成率：Z%

### 关键成果
- 成果1
- 成果2

### 下周计划
- 任务1
- 任务2
```

---

## 🎯 成功标准

### M6成功标准（标准库完成）

- ✅ 所有标准库函数实现完成（94/94函数）
- ✅ 单元测试覆盖率>90%
- ✅ 通过基础集成测试
- ✅ 高优先级TODO全部修复

### M7成功标准（1.0发布）

- ✅ 所有TODO修复完成
- ✅ 集成测试通过率>90%
- ✅ 性能达到Lua 5.1.5的70%以上
- ✅ 无已知严重bug
- ✅ 文档完善
- ✅ 发布说明完整

---

## 📚 参考资源

### 主要参考

1. **lua_c_analysis**（Lua 5.1.5源码+注释）
   - 路径：`../lua_c_analysis/src/`
   - 用途：实现参考、算法理解

2. **Lua 5.1 Reference Manual**
   - 用途：API规范、行为定义

3. **lua_with_cpp**（现代C++实现）
   - 路径：`../lua_with_cpp/src/`
   - 用途：架构参考、最佳实践

### 关键文档

- `lua_c_analysis/src/lbaselib.c` - 基础库实现
- `lua_c_analysis/src/lstrlib.c` - 字符串库实现
- `lua_c_analysis/src/ltablib.c` - 表库实现
- `lua_c_analysis/src/liolib.c` - I/O库实现
- `lua_c_analysis/src/loslib.c` - OS库实现

---

**路线图制定完成** ✅

**下一步行动**：开始执行P0任务，从任务1（LuaState初始化）开始。
